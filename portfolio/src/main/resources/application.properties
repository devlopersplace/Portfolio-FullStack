spring.application.name=portfolio

# Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/portfolio_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
# Put your actual MySQL password here
spring.datasource.password=Siliguri@123

# Hibernate ddl-auto (updates your schema automatically during development)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=true

# Port configuration (optional, defaults to 8080)
server.port=8080

# Spring Security Admin Credentials
spring.security.user.name=himanshu
spring.security.user.password=12345678